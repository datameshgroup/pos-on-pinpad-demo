plugins {
    id 'com.android.application'
    id 'kotlin-android'
}

android {
    compileSdk 32

    defaultConfig {
        applicationId "au.com.dmg.terminalposdemo"
        minSdk 21
        targetSdk 32
        versionCode 1
        versionName "2.0-fuel-products"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        ndk {
            abiFilters 'armeabi'
        }

        packagingOptions {
            jniLibs {
                useLegacyPackaging true
            }
        }
    }

    flavorDimensions 'device'
    productFlavors {
        pax {
            dimension "device"
            applicationId = "au.com.dmg.terminalposdemo"
            buildConfigField 'boolean', 'DEVICE_PAX', 'true'
            buildConfigField 'boolean', 'DEVICE_IGO', 'false'
            buildConfigField 'String', 'VERSION_TYPE', "\"v1\""

        }
        ingenico {
            dimension "device"
            applicationId = "au.com.dmg.terminalposdemo"
            buildConfigField 'boolean', 'DEVICE_PAX', 'false'
            buildConfigField 'boolean', 'DEVICE_IGO', 'true'
            buildConfigField 'String', 'VERSION_TYPE', "\"v1\""
        }
    }

    buildTypes {
        debug {
            debuggable = true
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['src/main/jniLibs']
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
        coreLibraryDesugaringEnabled true
    }

    buildFeatures {
        viewBinding true
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion '1.2.0'
    }
    kotlinOptions {
        jvmTarget = '1.8'
    }
    afterEvaluate {
        android.applicationVariants.all { variant ->
            variant.outputs.each { output ->
                def buildType = variant.buildType.name.capitalize()
                def flavor = variant.flavorName.capitalize()

                def assembleTaskName = "assemble${flavor}${buildType}"

                tasks.named(assembleTaskName).configure {
                    doLast {
                        println "Renaming APK files for $flavor $buildType..."

                        // Get the built APK file for the flavor
                        def apkFile = output.outputFile
                        if (apkFile.exists()) {
                            // Rename the APK file
                            def newApkName = "DebugTerminalPOSDemo${flavor}920" + android.productFlavors[flavor.toLowerCase()].buildConfigFields.VERSION_TYPE.value.replaceAll('^\"|\"$', "") + "." + android.defaultConfig.versionName + ".apk"
                            def newApk = file("${buildDir}/outputs/apk/${flavor.toLowerCase()}/${buildType.toLowerCase()}/$newApkName")
                            ant.move(file: apkFile, tofile: newApk)
                            println "Renamed APK file to: $newApk"
                        } else {
                            println "APK file not found for $flavor $buildType."
                        }
                    }
                }
            }
        }
    }


}
dependencies {
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:1.1.5'
    implementation 'androidx.appcompat:appcompat:1.4.2'
    implementation 'com.google.android.material:material:1.6.1'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'org.apache.commons:commons-lang3:3.12.0'
    implementation 'com.google.code.gson:gson:2.8.8'
    implementation "com.squareup.moshi:moshi:1.14.0"
    testImplementation 'junit:junit:4.13.2'

    implementation "com.datameshgroup.fusion:fusion-sdk:2.0.0"
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.6.1"
//    androidTestImplementation 'androidx.test.ext:junit:1.1.3'
//    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'

    // Printing utilities
    implementation "com.izettle:html2bitmap:1.10"
    ingenicoImplementation fileTree(include: ['*.jar'], dir: 'libs/ingenico')
    paxImplementation fileTree(include: ['*.jar'], dir: 'libs/pax')

    //added for new FuelProducts page
    implementation 'androidx.core:core-ktx:1.8.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.5.1'
    implementation 'androidx.activity:activity-compose:1.5.1'
    implementation 'androidx.compose.ui:ui:1.2.0'
    implementation 'androidx.compose.material:material:1.2.0'
//    implementation "androidx.compose.ui:ui-tooling-preview:1.1.0"
    implementation 'androidx.navigation:navigation-compose:2.5.1'
    debugImplementation 'androidx.compose.ui:ui-tooling:1.2.0'
    debugImplementation 'androidx.compose.ui:ui-test-manifest:1.2.0'
    androidTestImplementation 'androidx.compose.ui:ui-test-junit4:1.2.0'
    androidTestImplementation 'androidx.test.ext:junit:1.1.4'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'
}